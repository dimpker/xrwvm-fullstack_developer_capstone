GENERATE_SOURCEMAP=false
CI=false

# Production Environment Variables for Vercel Deployment

# Disable source map generation for production
GENERATE_SOURCEMAP=false

# Disable CI mode to prevent treating warnings as errors
CI=false

# Backend API URL - Django service deployed on Railway
# This URL will be used by all API calls when running in production
REACT_APP_BACKEND_URL=https://djangobackend-production-dd54.up.railway.app
