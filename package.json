{
  "name": "car-dealership-fullstack",
  "version": "1.0.0",
  "description": "Full-stack car dealership application",
  "scripts": {
    "build": "cd server/frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.x"
  }
}