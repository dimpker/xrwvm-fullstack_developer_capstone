{
  "name": "car-dealership-fullstack",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "cd server/frontend && npm install && npm run build && cd ../.. && mv server/frontend/build public"
  }
}